{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://starbridge.local/schemas/illustrator/realtime_command.v1.json",
  "title": "IllustratorRealtimeCommand",
  "type": "object",
  "required": ["jsonrpc", "id", "method", "params"],
  "additionalProperties": false,
  "properties": {
    "jsonrpc": {"const": "2.0"},
    "id": {"type": ["string", "integer"]},
    "method": {
      "enum": [
        "illustrator.get_state", "illustrator.document_info",
        "illustrator.select_object", "illustrator.set_fill",
        "illustrator.move_object", "illustrator.create_path",
        "illustrator.zoom_to_selection",
        "illustrator.apply_artisan_map", "illustrator.readback_artisan_map", "illustrator.commit_artisan_map",
        "illustrator.rollback_artisan_map"
      ]
    },
    "params": {
      "type": "object",
      "properties": {
        "object_id": {"type": "string", "pattern": "^item:[1-9][0-9]*$", "maxLength": 128},
        "confirm_write": {"type": "boolean"},
        "color": {"type": "string", "pattern": "^#[0-9A-Fa-f]{6}$"},
        "dx": {"type": "number", "minimum": -10000, "maximum": 10000},
        "dy": {"type": "number", "minimum": -10000, "maximum": 10000},
        "points": {
          "type": "array", "minItems": 2, "maxItems": 512,
          "items": {
            "type": "array", "prefixItems": [{"type": "number"}, {"type": "number"}],
            "items": false
          }
        },
        "closed": {"type": "boolean"}
        ,
        "transaction_ref": {"type": "string", "pattern": "^apply:[0-9a-f]{12}$"},
        "map_ref": {"type": "string", "pattern": "^imap:[0-9a-f]{12}$"},
        "expected_state_revision": {"type": "integer", "minimum": 1},
        "layers": {"$ref": "#/$defs/layerNameRows"},
        "objects": {"$ref": "#/$defs/objectNameRows"}
      },
      "additionalProperties": false
    }
  },
  "$defs": {
    "safeName": {"type": "string", "minLength": 1, "maxLength": 64, "pattern": "^[^\"'/:\\\\]+$"},
    "layerNameRows": {
      "type": "array", "maxItems": 4,
      "items": {
        "type": "array", "minItems": 2, "maxItems": 2,
        "prefixItems": [
          {"type": "string", "pattern": "^layer-[a-z][a-z-]{0,31}$"},
          {"$ref": "#/$defs/safeName"}
        ],
        "items": false
      }
    },
    "objectNameRows": {
      "type": "array", "maxItems": 128,
      "items": {
        "type": "array", "minItems": 2, "maxItems": 2,
        "prefixItems": [
          {"type": "string", "pattern": "^shape-[0-9]{4,}$"},
          {"$ref": "#/$defs/safeName"}
        ],
        "items": false
      }
    }
  }
}
